function [ out ] = quad_kernel( X_i,b )
%quad kernel
 m=length(X_i);
 D=quad_kernel_size(m);
 out =zeros(D,1) ;
 for i=1:m
     out(i)=X_i(i)^2;
 end   
 for i=2:m % This double sum in middle
       for j=1:i-1
           D=D+1;            
       end       
 end  
end